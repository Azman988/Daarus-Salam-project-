const selector = (query) => document.querySelector(query)
const selectorAll = (query) => document.querySelectorAll(query)
const selectId = (query) => document.getElementById(query)

export { selector, selectorAll, selectId };